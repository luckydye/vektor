---
import DocsContent from "#components/DocsContent.astro";
import Content, { getHeadings } from "#docs/extensions.md";
import DocsLayout from "#layouts/DocsLayout.astro";

const headings = getHeadings().filter((heading) => heading.depth === 2);
---

<DocsLayout title="Extensions - Vektor" headings={headings}>
  <DocsContent>
    <Content />
  </DocsContent>
</DocsLayout>
