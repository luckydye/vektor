{
  "name": "@vektor/native-image",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bun run ./build.ts",
    "build:debug": "bun run ./build.ts --debug"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  },
  "napi": {
    "name": "image"
  }
}
