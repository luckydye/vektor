node_modules
temp

env
.env
.env.*

*credentials*
# ...but the rule above is for secret files, and must not swallow source that
# merely says "credentials" in its name — a silently untracked module fails as
# "Cannot find module" on the next clone, not at the commit that dropped it.
!app/src/**/*credentials*

.claude
.zed

output

target

dist
*.zip

/extensions

.opencode-runtime

**/data/**/*

graphify-out

.task

api-example

*.log

# Downloaded at native-embedding build time and compiled into the addon.
/app/native/embedding/models/

# Generated platform-specific native embedding addon and its loader.
/app/src/search/native/

/app/test
