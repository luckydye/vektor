version: '3.8'

services:
  app:
    image: ${VEKTOR_IMAGE:-registry.s-v.de/tihav/wiki:latest}
    restart: unless-stopped
    env_file: env
    ports:
      - "8081:8080"
    volumes:
      - wiki_data:/app/data

volumes:
  wiki_data:
    driver: local
